services:
  bot:
    build: .
    env_file: .env
    restart: unless-stopped
    volumes:
      - ./state.json:/app/state.json   # persist seen-trades across restarts
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
